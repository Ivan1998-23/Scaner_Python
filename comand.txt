source venv/bin/activate
 python flsite.py
pip freeze > requirements.txt
навпаки завантажити: pip install -r requirements.txt


Масскан, по умолчанию, сканирует со скоростью 100 пакетов в секунду (медленно).
 Чтобы увеличить скорость, нужно использовать параметр --rate: 
sudo masscan 172.217.167.46 -p0-65535 --rate 25000000


Alembic
	alembic init migration





class LogScan(db.Model):
    __tablename__ = 'nmap'
    id = db.Column(db.Integer, primary_key=True) 
    start_time = db.Column(db.DateTime, default=datetime.now)           # дата створення
    ips = db.Column(db.String(25), nullable=False, unique=True)
    result = db.Column(db.String(255), nullable=True)           	 	#  result scan......
    other = db.Column(db.String(255), nullable=True)            		# nmap result scan......

    def __repr__(self):
        return f'<LogScan {self.id}>'


